# Helper repo to build caddy with https://github.com/mholt/caddy-l4

Builds `caddy` using [xcaddy](https://github.com/caddyserver/xcaddy) and puts it into a Docker image.

## Usage

```bash
./build.sh
```

## Output
 - standlone binary
 - Docker image